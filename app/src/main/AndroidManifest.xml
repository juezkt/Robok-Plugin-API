<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <uses-permission android:name="dev.juez.robok.plugin.api.permission.ACCESS_PERMISSION_SERVICE" />
  <application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:roundIcon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:supportsRtl="true">
    <activity
      android:name="dev.juez.plugin.MainActivity"
      android:exported="true">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>

    <service
      android:name="dev.juez.robok.plugin.api.PluginPermissionService"
      android:exported="true"
      android:permission="dev.juez.robok.plugin.api.permission.ALLOW_PLUGIN" />
  </application>
</manifest>